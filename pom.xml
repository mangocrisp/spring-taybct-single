<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>io.github.mangocrisp</groupId>
    <artifactId>spring-taybct-single</artifactId>
    <version>3.2.4</version>
    <name>Spring TayBct Single</name>
    <description>Spring TayBct Single</description>
    <packaging>pom</packaging>

    <properties>

        <!--框架依赖-->

        <!--central 发布插件-->
        <central-publishing-maven-plugin.version>0.5.0</central-publishing-maven-plugin.version>
        <!-- Collection 增强Java集合框架 -->
        <commons-collections4.version>4.4</commons-collections4.version>
        <!--commons-dbutils jdbc 工具类-->
        <commons-dbutils.version>1.8.1</commons-dbutils.version>
        <!--cmd exec-->
        <commons-exec.version>1.3</commons-exec.version>
        <!-- 文件上传工具类 -->
        <commons-fileupload.version>1.5</commons-fileupload.version>
        <!--commons-httpclient-->
        <commons-httpclient.version>3.1</commons-httpclient.version>
        <!--commons-lang3-->
        <commons-lang3.version>3.14.0</commons-lang3.version>
        <!-- 公共资源池 -->
        <commons-pool2.version>2.12.0</commons-pool2.version>
        <!-- io常用工具类 -->
        <commons-io.version>2.15.1</commons-io.version>
        <!--dom4j-->
        <dom4j.version>2.1.4</dom4j.version>
        <!--阿里巴巴(Druid)德鲁伊连接池-->
        <druid.version>1.2.21</druid.version>
        <!--dubbo 3-->
        <dubbo.version>3.2.12</dubbo.version>
        <!--dysmsapi阿里云短信-->
        <dysmsapi.version>2.0.1</dysmsapi.version>
        <!--多数据源-->
        <dynamic-datasource.version>4.3.0</dynamic-datasource.version>
        <!--easyexcel poi 操作导入导出 excel SAX-->
        <easyexcel.version>4.0.1</easyexcel.version>
        <!--easy poi DOM-->
        <easypoi.version>4.5.0</easypoi.version>
        <!--fastdfs的Java客户端框架-->
        <fastdfs-client.version>1.27.2</fastdfs-client.version>
        <!--fastjson2-->
        <fastjson2.version>2.0.50</fastjson2.version>
        <!--feign-httpclient-->
        <feign-httpclient.version>13.2.1</feign-httpclient.version>
        <!--feign-okhttp-->
        <feign-okhttp.version>13.2.1</feign-okhttp.version>
        <!--jackson-databind-->
        <jackson-databind.version>2.15.4</jackson-databind.version>
        <!--jackson-datatype-jsr310-->
        <jackson-datatype-jsr310.version>2.15.4</jackson-datatype-jsr310.version>
        <!--jdk 版本-->
        <java.version>17</java.version>
        <!--jakarta.servlet-api-->
        <jakarta.servlet-api.version>6.0.0</jakarta.servlet-api.version>
        <!--加密工具-->
        <jdk15on.version>1.70</jdk15on.version>
        <jdk18on.version>1.77</jdk18on.version>
        <!--junit 测试-->
        <junit.version>4.13.2</junit.version>
        <!--poi 导出依赖-->
        <jxls-core.version>1.0.3</jxls-core.version>
        <!--httpclient5-->
        <httpclient5.version>5.2.3</httpclient5.version>
        <!--java 工具类库-->
        <hutool.version>5.8.27</hutool.version>
        <!--knife4j-->
        <knife4j.version>4.5.0</knife4j.version>
        <lombok.version>1.18.32</lombok.version>
        <maven.compiler.source>17</maven.compiler.source>
        <maven.compiler.target>17</maven.compiler.target>
        <!--maven 插件-->
        <maven-compiler-plugin.version>3.13.0</maven-compiler-plugin.version>
        <!--Gpg Signature 插件-->
        <maven-gpg-plugin.version>3.2.5</maven-gpg-plugin.version>
        <!-- javadoc插件 -->
        <maven-javadoc-plugin.version>3.8.0</maven-javadoc-plugin.version>
        <!--maven 源码插件-->
        <maven-source-plugin.version>3.3.1</maven-source-plugin.version>
        <!--采用 Annotation Processor 技术，自动生成 java spi 和 Spring boot starter 的配置-->
        <mica-auto.version>3.1.3</mica-auto.version>
        <!--minio 对象存储-->
        <minio.version>8.5.2</minio.version>
        <!--mybatis-plus-->
        <mybatis-plus.version>3.5.6</mybatis-plus.version>
        <!--mybatis-spring-->
        <mybatis-spring.version>3.0.3</mybatis-spring.version>
        <!--mybatis-typehandlers-jsr310-->
        <mybatis-typehandlers-jsr310.version>1.0.2</mybatis-typehandlers-jsr310.version>
        <!--mysql-->
        <mysql-connector-j.version>8.3.0</mysql-connector-j.version>
        <!--nacos 客户端-->
        <nacos-client.version>2.3.2</nacos-client.version>
        <!--JWT 解析库-->
        <nimbus-jose-jwt.version>9.39</nimbus-jose-jwt.version>
        <!--阿里云对象存储-->
        <oss-alibaba.version>3.16.1</oss-alibaba.version>
        <!--oracle-->
        <ojdbc8.version>21.9.0.0</ojdbc8.version>
        <!--oracle字符集支持-->
        <orai18n.version>21.9.0.0</orai18n.version>
        <!--对中文多音字处理-->
        <pinyin4j.version>2.5.1</pinyin4j.version>
        <!--    postgresql 驱动   -->
        <postgresql.version>42.7.3</postgresql.version>
        <!--项目打包编码设置-->
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <!-- Sentinel规则持久化至Nacos配置 -->
        <sentinel-datasource-nacos.version>1.8.6</sentinel-datasource-nacos.version>
        <!--基础工具版本-->
        <project.version>3.2.4</project.version>
        <!--skywalking 日志记录-->
        <skywalking-logback.version>8.14.0</skywalking-logback.version>
        <!--Spring Context Support cache mail scheduling-->
        <spring-context-support.version>6.1.6</spring-context-support.version>
        <!--管理和监视Spring Boot的应用程序-->
        <spring-boot-admin.version>3.2.3</spring-boot-admin.version>
        <!--spring boot 版本依赖-->
        <spring-boot-maven-plugin.version>3.2.5</spring-boot-maven-plugin.version>
        <!--spring boot 版本依赖-->
        <spring-boot.version>3.2.5</spring-boot.version>
        <!--OAuth2 认证服务器-->
        <spring-security-config.version>6.2.4</spring-security-config.version>
        <!--OAuth2 认证服务器-->
        <spring-security-oauth2-authorization-server.version>1.2.4</spring-security-oauth2-authorization-server.version>
        <!--OAuth2 资源服务器-->
        <spring-security-oauth2-resource-server.version>6.2.4</spring-security-oauth2-resource-server.version>
        <spring-security-oauth2-client.version>6.2.4</spring-security-oauth2-client.version>
        <spring-security-oauth2-jose.version>6.2.4</spring-security-oauth2-jose.version>
        <!--spring-test-->
        <spring-test.version>6.1.6</spring-test.version>
        <!--spring-tx-->
        <spring-tx.version>6.1.6</spring-tx.version>
        <!--spring-cloud-context-->
        <spring-cloud-context.version>4.1.2</spring-cloud-context.version>
        <!--这里加密工具需要-->
        <spring-security-crypto.version>6.2.4</spring-security-crypto.version>
        <!--spring-security-rsa-->
        <spring-security-rsa.version>1.1.2</spring-security-rsa.version>
        <!--gateway 网关-->
        <spring-cloud-starter-gateway.version>4.1.2</spring-cloud-starter-gateway.version>
        <!--bootstrap 启动器-->
        <spring-cloud-starter-bootstrap.version>4.1.2</spring-cloud-starter-bootstrap.version>
        <!-- spring 客户端负载均衡器 -->
        <spring-cloud-starter-loadbalancer.version>4.1.2</spring-cloud-starter-loadbalancer.version>
        <!-- SpringCloud Openfeign 服务调用 -->
        <spring-cloud-starter-openfeign.version>4.1.1</spring-cloud-starter-openfeign.version>
        <!--spring cloud 微服务版本依赖-->
        <spring-cloud.version>2023.0.1</spring-cloud.version>
        <!--阿里巴巴版本依赖-->
        <spring-cloud-alibaba.version>2023.0.1.0</spring-cloud-alibaba.version>
        <!--Spring web-->
        <spring-web.version>6.1.6</spring-web.version>
        <!--spring-webmvc-->
        <spring-webmvc.version>6.1.6</spring-webmvc.version>
        <!--swagger openapi3.0 注解-->
        <swagger-annotations-jakarta.version>2.2.21</swagger-annotations-jakarta.version>

        <!--项目需要-->

        <dockerfile-maven-plugin.version>1.4.13</dockerfile-maven-plugin.version>
        <!--Registry2仓库的地址，ip:port-->
        <docker.repository.url>127.0.0.1:5000</docker.repository.url>
        <!--<docker.repository.url>registry.cn-beijing.aliyuncs.com</docker.repository.url>-->
        <!--项目名-->
        <docker.registry.name>spring-taybct-single</docker.registry.name>
        <docker.registry.username>taybct</docker.registry.username>
        <docker.registry.password>taybct</docker.registry.password>
        <!--maven 打包工具，可以把各个模块打的包移动到同一个 target 文件夹下面方便部署-->
        <maven-antrun-plugin.version>3.1.0</maven-antrun-plugin.version>
        <!--生成 archetype 模板插件-->
        <maven-archetype-plugin.version>3.2.1</maven-archetype-plugin.version>
        <!--工具版本-->
        <spring-taybct-tools.version>3.2.4</spring-taybct-tools.version>
    </properties>

    <!-- 依赖声明 -->
    <dependencyManagement>
        <dependencies>

            <!--依赖版本管理-->
            <dependency>
                <groupId>io.github.mangocrisp</groupId>
                <artifactId>spring-taybct-tools-dependencies</artifactId>
                <version>${spring-taybct-tools.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!--运行模块-鉴权模块-->
            <dependency>
                <groupId>io.github.mangocrisp</groupId>
                <artifactId>auth</artifactId>
                <version>${project.version}</version>
            </dependency>
            <!--运行模块-文件管理-->
            <dependency>
                <groupId>io.github.mangocrisp</groupId>
                <artifactId>admin-file</artifactId>
                <version>${project.version}</version>
            </dependency>
            <!--运行模块-日志管理-->
            <dependency>
                <groupId>io.github.mangocrisp</groupId>
                <artifactId>admin-log</artifactId>
                <version>${project.version}</version>
            </dependency>
            <!--运行模块-系统模块-->
            <dependency>
                <groupId>io.github.mangocrisp</groupId>
                <artifactId>module-system</artifactId>
                <version>${project.version}</version>
            </dependency>
            <!--任务调度模块-->
            <dependency>
                <groupId>io.github.mangocrisp</groupId>
                <artifactId>module-scheduling</artifactId>
                <version>${project.version}</version>
            </dependency>
            <!--流程引擎-->
            <dependency>
                <groupId>io.github.mangocrisp</groupId>
                <artifactId>module-lf</artifactId>
                <version>${project.version}</version>
            </dependency>
            <!--在线文档-->
            <dependency>
                <groupId>io.github.mangocrisp</groupId>
                <artifactId>module-online-doc</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!--api 接口-系统-->
            <dependency>
                <groupId>io.github.mangocrisp</groupId>
                <artifactId>api-system</artifactId>
                <version>${project.version}</version>
            </dependency>
            <!--公共模块（偏业务）-->
            <dependency>
                <groupId>io.github.mangocrisp</groupId>
                <artifactId>common</artifactId>
                <version>${project.version}</version>
            </dependency>

        </dependencies>
    </dependencyManagement>

    <dependencies>

        <!--配置文件处理器的依赖-->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>
        <!-- lombok getter setter slf4j -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>
        <!--java 工具类库-->
        <dependency>
            <groupId>cn.hutool</groupId>
            <artifactId>hutool-all</artifactId>
        </dependency>
        <!--自动生成 spring.factories 配置-->
        <dependency>
            <groupId>net.dreamlu</groupId>
            <artifactId>mica-auto</artifactId>
            <scope>provided</scope>
        </dependency>

    </dependencies>

    <modules>
        <module>run</module>
        <module>api</module>
        <module>modules</module>
        <module>admin</module>
        <module>auth</module>
        <module>common</module>
        <module>laboratory</module>
    </modules>

    <build>
        <finalName>${project.artifactId}-${project.version}</finalName>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
            </resource>
            <resource>
                <directory>src/main/java</directory>
                <includes>
                    <include>**/*.xml</include>
                </includes>
            </resource>
        </resources>
        <!--设置子项目不需要指定maven插件版本-->
        <pluginManagement>
            <plugins>
                <!--spring boot 打包插件-->
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring-boot-maven-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${maven-compiler-plugin.version}</version>
                </plugin>
                <!--打包源码插件-->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-source-plugin</artifactId>
                    <version>${maven-source-plugin.version}</version>
                </plugin>
                <!--maven 打包工具-->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-antrun-plugin</artifactId>
                    <version>${maven-antrun-plugin.version}</version>
                </plugin>
                <!--生成 maven 模板-->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-archetype-plugin</artifactId>
                    <version>${maven-archetype-plugin.version}</version>
                </plugin>
                <!--docker 打包-->
                <plugin>
                    <groupId>com.spotify</groupId>
                    <artifactId>dockerfile-maven-plugin</artifactId>
                    <version>${dockerfile-maven-plugin.version}</version>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <!--spring boot 打包插件-->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <finalName>${project.build.finalName}</finalName>
                    <!-- spring-boot:run 中文乱码解决 -->
                    <jvmArguments>-Dfile.encoding=UTF-8</jvmArguments>
                    <!--打包本地 jar 包到-->
                    <includeSystemScope>true</includeSystemScope>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <!-- 指定maven编译的jdk版本,如果不指定,maven3默认用jdk 1.5 maven2默认用jdk1.3 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <!-- 一般而言，target与source是保持一致的，但是，有时候为了让程序能在其他版本的jdk中运行(对于低版本目标jdk
                    ，源代码中不能使用低版本jdk中不支持的语法)，会存在target不同于source的情况 -->
                    <!-- 源代码使用的JDK版本 -->
                    <source>${java.version}</source>
                    <!-- 需要生成的目标class文件的编译版本 -->
                    <target>${java.version}</target>
                    <!-- 字符集编码 -->
                    <encoding>${project.build.sourceEncoding}</encoding>
                    <showWarnings>true</showWarnings>
                    <compilerArgs>
                        <arg>-parameters</arg>
                    </compilerArgs>
                </configuration>
            </plugin>
            <!--打包源码插件-->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>compile</phase>
                        <goals>
                            <goal>jar-no-fork</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <!--生成 maven 模板-->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-archetype-plugin</artifactId>
            </plugin>
            <!-- 构建和推动Docker镜像 -->
            <plugin>
                <groupId>com.spotify</groupId>
                <artifactId>dockerfile-maven-plugin</artifactId>
                <configuration>
                    <!--如果需要自动打包 docker 镜像，就注释这个标签 skip-->
                    <skip>true</skip>
                    <contextDirectory>${maven.multiModuleProjectDirectory}</contextDirectory>
                    <useMavenSettingsForAuth>true</useMavenSettingsForAuth>
                    <!--仓库地址/项目名/模块名-->
                    <repository>${docker.repository.url}/${docker.registry.name}/${project.build.finalName}</repository>
                    <!--用户名-->
                    <username>${docker.registry.username}</username>
                    <!--用户密码-->
                    <password>${docker.registry.password}</password>
                    <tag>${project.version}</tag>
                    <buildArgs>
                        <JAR_FILE>target/${project.build.finalName}.jar</JAR_FILE>
                    </buildArgs>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>